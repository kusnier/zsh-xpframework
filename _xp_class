#autoload

local i expl
local -a c

  for i in `cat *.pth`; do
    if [[ -d $i ]]; then
      c+=( $i/**/*.class.php(.:r:s/.class//:s#$i/##:gs#/#.#) )
    fi
  done
  _wanted classes expl 'xp-framework class' compadd "$@" -M 'r:|.=* r:|=*' -a - c
